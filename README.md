1. make changes in `index.html` in vault
2. "[Enhancing Export to ...](https://github.com/mokeyish/obsidian-enhancing-export)" ... `html` to root dir
3. run `staticrypt index.html` & make sure the unencrypted `index.html` is overwritten
4. commit & push changes
